services:
  mysql:
    image: ${MYSQL_IMAGE}
    container_name: crm-dev-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      - "${MYSQL_PORT:-33060}:3306"
    volumes:
      - mysql_dev_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - crm_network

  app:
    build:
      context: ../..
      dockerfile: infra/development/app/Dockerfile
      args:
        PHP_IMAGE: ${PHP_IMAGE}
        APP_USER: ${APP_USER}
        APP_GROUP: ${APP_GROUP}
        APP_UID: ${APP_UID}
        APP_GID: ${APP_GID}
    container_name: crm-dev-app
    restart: unless-stopped
    working_dir: /var/www/crm
    env_file:
      - ../../services/crm/.env
    environment:
      APP_ENV: ${APP_ENV}
      APP_DEBUG: ${APP_DEBUG}
      APP_URL: ${APP_URL}
      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: ${MYSQL_DATABASE}
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ../../services/crm:/var/www/crm
      - crm_dev_vendor:/var/www/crm/vendor
      - crm_dev_storage:/var/www/crm/storage
      - crm_dev_bootstrap_cache:/var/www/crm/bootstrap/cache
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 9000"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    networks:
      - crm_network

  nginx:
    image: ${NGINX_IMAGE}
    container_name: crm-dev-nginx
    restart: unless-stopped
    depends_on:
      app:
        condition: service_healthy
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - ../../services/crm:/var/www/crm:ro
      - crm_dev_vendor:/var/www/crm/vendor:ro
      - crm_dev_storage:/var/www/crm/storage:ro
      - crm_dev_bootstrap_cache:/var/www/crm/bootstrap/cache:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://127.0.0.1/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - crm_network

volumes:
  mysql_dev_data:
  crm_dev_vendor:
  crm_dev_storage:
  crm_dev_bootstrap_cache:

networks:
  crm_network:
    name: crm-dev-network
